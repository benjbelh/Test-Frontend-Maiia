import React from 'react';
import { render } from '@testing-library/react';
import ProductCard from '../ProductCard';

describe('ProductCard', () => {
  // it('should renders correctly by default', () => {
  //   const { asFragment } = render(<ProductCard />);
  //   expect(asFragment()).toMatchSnapshot();
  // });
  // it('should renders correctly with card props', () => {
  //   const { asFragment } = render(
  //     <ProductCard id="test-id" name="Product Name" picturePath="https://test.com/img.jpg" location="Cannes" />,
  //   );
  //   expect(asFragment()).toMatchSnapshot();
  // });
});
